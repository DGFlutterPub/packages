import 'package:flutter/material.dart';

class TextConfig {
  double size;
  Color? color;
  FontWeight? weight;
  int? maxLine;
  TextConfig({this.color, this.size = 14, this.weight, this.maxLine});
}
